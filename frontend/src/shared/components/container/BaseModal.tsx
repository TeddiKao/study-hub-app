import type { ReactNode } from "react";

interface BaseModalProps {
    visible: boolean;
    children: ReactNode;
}

function BaseModal({ visible, children }: BaseModalProps) {
    
}

export default BaseModal;